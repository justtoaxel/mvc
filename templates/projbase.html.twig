<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="{{ asset('img/glider.svg') }}">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app2') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app2') }}
        {% endblock %}
    </head>
    <body>
        <nav>
            <ul>
                <li><a href="{{ path('home') }}">Home</a></li>
                <li><a href="{{ path('about') }}">Report</a></li>
                <li class="split"><a href="{{ path('proj') }}">Restart Game</a></li>
                <li class="split"><a href="{{ path('proj_api') }}">API</a></li>
                <li class="split"><a href="{{ path('proj_about') }}">About</a></li>
            </ul>
        </nav>

        <main>{% block body %}{% endblock %}</main>
    </body>
</html>
